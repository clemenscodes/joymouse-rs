tasks:
  watch:
    command: 'pnpm watch'
    local: true
  dev:
    command: 'pnpm start'
    local: true
    deps:
      - "watch"
  build:
    command: 'pnpm build'
    inputs:
      - 'src/**/*'
      - 'styles.css'
      - 'package.json'
    outputs:
      - 'dist/ui'